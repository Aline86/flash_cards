{% extends 'base.html.twig' %}

{% block title %}Allemand{% endblock %}

{% block body %}
    <h1>Allemand</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ allemand.id }}</td>
            </tr>
            <tr>
                <th>Fr</th>
                <td>{{ allemand.fr }}</td>
            </tr>
            <tr>
                <th>De</th>
                <td>{{ allemand.de }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('allemand_index') }}">back to list</a>

    <a href="{{ path('allemand_edit', {'id': allemand.id}) }}">edit</a>

    {{ include('allemand/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Russe{% endblock %}

{% block body %}
    <div class="form">
        <h1>Allemand</h1>
        <form id="theme_form" class="form">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn">{{ button_label|default('Save') }}</button>
            {{ form_end(form) }}
        </form>
    </div>
        <div id="result">
        {% if allemands is defined  %}
        {% for allemand in allemands %}
            <div id="fr">
                <div class="center">
                    <div id="french">
                        <div>{{ allemand.fr }}</div>
                        <input id="button" type="submit" value="Traduction" />                       
                    </div>
                </div>
                <div class="center">                   
                    <div id="russian">
                        <div>{{ allemand.de }}</div>
                    </div>
                </div>              
               </div>        
            </div>    
        {% endfor %}
        <div class="pagination">
        {{knp_pagination_render(allemands)}}
        </div>
        {% endif %}   
          
{% endblock %}
